{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "npm install -g pnpm@10 && cd ../school-website && pnpm install --no-frozen-lockfile --ignore-workspace",
  "buildCommand": "cd ../school-website && pnpm run build && mkdir -p ../api-server/dist && cp -r dist/public/* ../api-server/dist/",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
